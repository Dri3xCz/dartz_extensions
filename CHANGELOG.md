# Changelog

## 0.1.0 - 2026-02-17
### Features
- `zip` and `zipLeft` for combining `Either` values into `Tuple2`.
- `tap`, `tapLeft`, and `tapBoth` for performing side effects without altering the original `Either`.
- `leftFlatMap` for flat mapping on the `Left` side of `Either`.

